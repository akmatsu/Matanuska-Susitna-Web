{
  "name": "cms",
  "version": "1.0.3",
  "private": true,
  "scripts": {
    "dev": "keystone dev --no-db-push",
    "start": "keystone start",
    "build": "keystone build && keystone prisma migrate deploy",
    "fix": "keystone postinstall --fix",
    "db:generate": "keystone prisma generate --binary-targets native,rhel-openssl-3.0.x",
    "db:migrations": "keystone prisma migrate dev"
  },
  "dependencies": {
    "@dotenvx/dotenvx": "^1.14.1",
    "@keystone-6/auth": "^8.0.0",
    "@keystone-6/core": "^6.3.0",
    "@keystone-6/fields-document": "^9.1.0",
    "typescript": "^5.6.2"
  }
}
