// This file is automatically generated by Keystone, do not modify it manually.
// Modify your Keystone config when you want to change this.

datasource postgresql {
  url               = env("DATABASE_URL")
  shadowDatabaseUrl = env("SHADOW_DATABASE_URL")
  provider          = "postgresql"
}

generator client {
  binaryTargets = ["native", "rhel-openssl-3.0.x"]
  provider      = "prisma-client-js"
}

model User {
  id        String    @id @default(cuid())
  name      String    @default("")
  email     String    @unique @default("")
  password  String
  createdAt DateTime? @default(now())
  updatedAt DateTime? @default(now()) @updatedAt
}

model Service {
  id            String         @id @default(cuid())
  title         String         @default("")
  description   String         @default("")
  publishAt     DateTime?
  unpublishAt   DateTime?
  content       Json           @default("[{\"type\":\"paragraph\",\"children\":[{\"text\":\"\"}]}]")
  externalLinks ExternalLink[] @relation("Service_externalLinks")
  processes     Process[]      @relation("Process_service")
  tags          Tag[]          @relation("Service_tags")
  createdAt     DateTime?      @default(now())
  updatedAt     DateTime?      @default(now()) @updatedAt
}

model ExternalLink {
  id                         String    @id @default(cuid())
  label                      String    @default("")
  url                        String    @default("")
  createdAt                  DateTime? @default(now())
  updatedAt                  DateTime? @default(now()) @updatedAt
  from_Service_externalLinks Service[] @relation("Service_externalLinks")
}

model Tag {
  id       String    @id @default(cuid())
  name     String    @default("")
  services Service[] @relation("Service_tags")
}

model Alert {
  id        String    @id @default(cuid())
  title     String    @default("")
  message   Json      @default("[{\"type\":\"paragraph\",\"children\":[{\"text\":\"\"}]}]")
  urgency   Int
  createdAt DateTime? @default(now())
  updatedAt DateTime? @default(now()) @updatedAt
}

model Process {
  id        String    @id @default(cuid())
  name      String    @default("")
  service   Service?  @relation("Process_service", fields: [serviceId], references: [id])
  serviceId String?   @map("service")
  steps     Step[]    @relation("Step_process")
  createdAt DateTime? @default(now())
  updatedAt DateTime? @default(now()) @updatedAt

  @@index([serviceId])
}

model Step {
  id        String    @id @default(cuid())
  label     String    @default("")
  process   Process?  @relation("Step_process", fields: [processId], references: [id])
  processId String?   @map("process")
  content   Json      @default("[{\"type\":\"paragraph\",\"children\":[{\"text\":\"\"}]}]")
  createdAt DateTime? @default(now())
  updatedAt DateTime? @default(now()) @updatedAt

  @@index([processId])
}
