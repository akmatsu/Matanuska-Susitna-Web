{
  "name": "craft-dmeo",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "msb:dev": "pnpm turbo run dev --filter=msb",
    "msb:build": "pnpm turbo run build --filter=msb",
    "msb:start": "pnpm turbo run start --filter=msb",
    "mde:build": "pnpm turbo run build --filter=@msb/md-editor",
    "myp:dev": "pnpm turbo run dev --filter=myprop",
    "myp:build": "pnpm turbo run build --filter=myprop",
    "myp:start": "pnpm turbo run start --filter=myprop",
    "ui:dev": "pnpm turbo run storybook --filter=@matsugov/ui",
    "ui:start": "pnpm turbo run build -- -w --filter=@matsugov/ui",
    "ui:build": "pnpm turbo run build --filter=@matsugov/ui",
    "wid:dev": "pnpm turbo run dev --filter=widgets",
    "wid:build": "pnpm turbo run build --filter=widgets",
    "wid:start": "pnpm turbo run start --filter=widgets",
    "lint": "eslint .",
    "lint:errors": "eslint . --quiet",
    "lint:fix": "eslint . --fix",
    "codegen": "pnpm turbo run codegen --filter=@msb/js-sdk",
    "codegen:watch": "pnpm turbo run codegen:watch --filter=@msb/js-sdk"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@eslint/eslintrc": "catalog:",
    "@eslint/js": "catalog:",
    "@semantic-release/changelog": "catalog:",
    "@semantic-release/commit-analyzer": "catalog:",
    "@semantic-release/git": "catalog:",
    "@semantic-release/github": "catalog:",
    "@semantic-release/release-notes-generator": "catalog:",
    "eslint": "catalog:",
    "eslint-config-flat-gitignore": "catalog:",
    "eslint-config-next": "catalog:",
    "globals": "catalog:",
    "prettier": "catalog:",
    "prettier-plugin-tailwindcss": "catalog:",
    "semantic-release": "catalog:",
    "turbo": "catalog:",
    "typescript": "catalog:",
    "typescript-eslint": "catalog:"
  },
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@11.3.0"
}
